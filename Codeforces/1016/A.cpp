#include<bits/stdc++.h>
#define MAX 10000
using namespace std;
typedef long long ll;
int main () {
    int n;
    cin >> n;
    int ar[MAX];

    for (int i = 0; i < n; i++) {
        cout << ar[i];
    }

    return 0;
}

